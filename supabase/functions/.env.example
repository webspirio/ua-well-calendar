# Copy to .env.local in the same folder for local `supabase functions serve`.
# In production these are set via `npx supabase secrets set KEY=value`.
BOT_TOKEN=
BOT_USERNAME=
FORUM_CHAT_ID=
PUBLIC_BASE_URL=https://<you>.github.io/calendar-app-tg/
